import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Exercice_4_MyForm',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
